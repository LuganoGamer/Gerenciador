name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.3.2

dependencies:
  date_format: ^2.0.9
  flutter:
    sdk: flutter
  intl: ^0.19.0
  sqflite: ^2.3.3+2

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
